# Server configuration
server.port=8080

# PostgreSQL Database configuration
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/woodcraft}
spring.datasource.username=${DB_USERNAME:woodcraft_user}
spring.datasource.password=${DB_PASSWORD:woodcraft2024}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Logging
logging.level.com.woodcraft=DEBUG

# OAuth2 Configuration
# Set these as environment variables or use defaults for development
oauth2.google.client-id=${GOOGLE_CLIENT_ID:1053231475525-7rn5ap1op0qg3igej8d5p17okunr551p.apps.googleusercontent.com}
oauth2.google.client-secret=${GOOGLE_CLIENT_SECRET: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}
oauth2.google.redirect-uri=http://localhost:3000/auth-callback.html

# Hugging Face AI Configuration
# Set HUGGINGFACE_API_TOKEN environment variable (required)
huggingface.api.token=${HUGGINGFACE_API_TOKEN:your-token-here}
huggingface.api.url=https://router.huggingface.co/hf-inference/models/black-forest-labs/FLUX.1-schnell
huggingface.timeout.seconds=60

# Furniture Preview Cache Configuration
furniture.preview.cache.enabled=true
furniture.preview.cache.max-size=100
